[Database]
username = root
password = root123
database = grafana_dashboards

[Table]
table_name = radio_link_inventory

[Fields]
id = Integer(primary_key=True, autoincrement=True)
radioLink = String(255)
linkDistance(km) = String(255)
neId = String(255)
neName = String(255)
region = String(255)
userLabel = String(255)
mnemonic = String(255)
partNumber = String(255)
TxFrequency(Ghz) = Float
RxFrequency(Ghz) = Float
band(GHz) = String(255)
maxCapacity(Mbps) =  Float
minCapacity(Mbps) =  Float
inLag= String(255)
